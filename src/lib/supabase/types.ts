/**
 * Tipos de la base de datos.
 *
 * Se regeneran desde el esquema real con:
 *   npm run db:types
 *
 * Mientras no exista el esquema, `Database` queda abierto para que el
 * SDK tipe sin bloquear. Al generar los tipos, este archivo se sobrescribe.
 */

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
